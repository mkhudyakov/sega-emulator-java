out/
build/
.gradle/
*.class
# Mega Drive ROMs use the .md extension, so ignore *.md — but keep the
# project's own markdown docs (they are not ROMs).
*.md
!README.md
!CLAUDE.md
!TUTORIAL.md
!PLAN.md
*.bin
*.gen
*.smd
.DS_Store
.idea/
